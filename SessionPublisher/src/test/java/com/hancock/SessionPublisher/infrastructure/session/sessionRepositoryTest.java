package com.hancock.SessionPublisher.infrastructure.session;

import com.hancock.SessionPublisher.session.SessionDomain;
import org.junit.Assert;
import org.junit.Test;
import org.springframework.beans.factory.annotation.Autowired;

public class sessionRepositoryTest {

    @Test
    public void should_when_given() {
//        //given
//        String title = "title";
//        String subTitle = "sub title";
//        int currentStage = 0;
//        int totalStage = 3;
//        SessionDomain sessionDomain = new SessionDomain(title, subTitle, currentStage, totalStage);
//        String domainId = sessionDomain.getId();
//        repository.save(sessionDomain);
//
//        //when
//        sessionDomain = repository.findById("id");
//
//        //then
//        Assert.assertNotNull(sessionDomain);
//        Assert.assertEquals(domainId, sessionDomain.getId());
    }
}